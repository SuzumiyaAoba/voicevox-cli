# Automatically load the nix development environment
# This file enables direnv to automatically activate the nix shell
# when you enter this directory

# Use the default development shell from flake.nix
use flake

# Alternative: use the voicevox shell with additional tools
# use flake .#voicevox

# Load any local environment variables
if [ -f .env.local ]; then
  dotenv .env.local
fi

# Set project-specific environment variables
export PROJECT_ROOT=$PWD
export VOICEVOX_CLI_CONFIG_DIR="$HOME/.config/voicevox-cli"

# Create config directory if it doesn't exist
mkdir -p "$VOICEVOX_CLI_CONFIG_DIR"

echo "🚀 VOICEVOX CLI development environment loaded"
